using System;
using System.Timers;

namespace Timer
{
    class Program
    {
        static void Main(string[] args)
        {
            /*int godziny = 0;
            int minuty = 0;
            int sekundy = 0;
            while (true)
            {
                Thread.Sleep(1000);
                Console.Clear();
                if (sekundy>=60&&minuty+1>=60)
                {
                    godziny+=1;
                    minuty = 0;
                    sekundy = 0;
                }
                else if(sekundy>=60&&minuty+1<60)
                {
                    minuty+=1;
                    sekundy=0;
                }
                System.DateTimeOffset
                System.TimeSpan
                System.Timers.Timer
                sekundy+=1;
            }*/
            /*System.Timers.Timer aTimer = new System.Timers.Timer(1000);
            System.Console.WriteLine(aTimer);*/
        }
    }
}
